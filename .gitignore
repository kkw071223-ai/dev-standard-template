# generated environments
.venv-ov/
.venv-simready/
.env.ov

# pipeline output
.out/

# installed skills (re-add with: npx skills add nvidia/skills ...)
# `skills` also drops its source checkout and lock file next to them.
.claude/skills/
.agents/
skills-lock.json

# caches
__pycache__/
*.py[cod]
.cache/

# figure intermediates (PNG masters are kept; webp is derived at build time)
docs/figures/*.webp

# exported hand-off copies (regenerate: scripts/export_asset.py)
deliverables/
